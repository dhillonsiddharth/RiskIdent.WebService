apiVersion: v1
kind: Service
metadata:
  name: {{ include "riskidentwebservicechart.fullname" . }}
  labels:
    {{- include "riskidentwebservicechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "riskidentwebservicechart.selectorLabels" . | nindent 4 }}
