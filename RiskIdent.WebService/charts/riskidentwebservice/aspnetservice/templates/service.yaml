apiVersion: v1
kind: Service
metadata:
  name: {{ include "aspnetservice.fullname" . }}
  labels:
    {{- include "aspnetservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aspnetservice.selectorLabels" . | nindent 4 }}
