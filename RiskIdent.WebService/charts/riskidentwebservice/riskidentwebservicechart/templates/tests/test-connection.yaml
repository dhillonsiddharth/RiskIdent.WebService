apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "riskidentwebservicechart.fullname" . }}-test-connection"
  labels:
{{ include "riskidentwebservicechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "riskidentwebservicechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
