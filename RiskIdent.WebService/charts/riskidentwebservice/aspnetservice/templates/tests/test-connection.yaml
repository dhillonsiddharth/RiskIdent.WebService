apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aspnetservice.fullname" . }}-test-connection"
  labels:
{{ include "aspnetservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "aspnetservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
