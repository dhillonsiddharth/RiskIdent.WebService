apiVersion: v1
kind: Service
metadata:
  name: {{ template "riskidentwebservice.fullname" . }}
  labels:
    app: {{ template "riskidentwebservice.name" . }}
    chart: {{ template "riskidentwebservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "riskidentwebservice.name" . }}
    release: {{ .Release.Name }}
